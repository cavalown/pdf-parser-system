# Backend Rules

這份文件記錄 backend 專屬規範。

## Stack

- Backend 使用 NestJS、TypeScript。

## API

- Backend API 都需要提供 property 說明。
- Backend API 都需要提供 controller 說明。
- API behavior 應保持明確，並在可行時以聚焦 tests 覆蓋。
- 使用 NestJS module、provider、controller 和 service patterns，避免 ad hoc structure。
