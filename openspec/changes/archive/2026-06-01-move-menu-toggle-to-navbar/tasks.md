## 1. Frontend

- [x] 1.1 更新 `frontend/src/App.tsx`，將 menu toggle button 從 sidebar 移到 top navbar。
- [x] 1.2 調整 navbar markup，使 menu toggle button 立即位於 `PDF Fac.` brand link 左邊。
- [x] 1.3 保留 menu toggle button 對 `sidebarOpen` state 的控制行為。
- [x] 1.4 移除 sidebar 內不再使用的 control row markup。
- [x] 1.5 更新 `frontend/src/App.css`，讓 navbar 左側以水平排列呈現 menu icon 與 `PDF Fac.`。
- [x] 1.6 清理或調整不再使用的 `.sidebar-control` 相關樣式。
- [x] 1.7 更新 navbar CSS，使 top navbar 使用暗紅色背景，並維持 menu icon 與 `PDF Fac.` 的文字對比。
- [x] 1.8 確認 sidebar 仍顯示 `PDF Analyze`、`PDF Modify`、`Setting` 並保留收合後 icon/label 行為。

## 2. Validation

- [x] 2.1 執行 frontend lint，確認 React、TypeScript 與 ESLint 規則通過。
- [x] 2.2 執行 frontend build，確認 production build 通過。
- [x] 2.3 以本機 dev server 檢查 navbar menu icon 位於 `PDF Fac.` 左邊，且點擊後可切換 sidebar。
